

describe('read from database', () => {
  it('should return true if data is read', () => {
    true.should.eql(false)
  })
})

describe('write to database', () => {
  it('should return true if data is written', () => {
    true.should.eql(false)
  })
})

